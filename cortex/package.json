{
  "name": "@tupynambalucas/cortex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "author": "Tupynambá Lucas Varela Rodrigues <tupynambalucas.dev>",
  "description": "Tupynambá Lucas Unified AI Cortex Workspace",
  "license": "https://github.com/tupynambalucas/tupynambalucas/tree/main#license",
  "homepage": "https://github.com/tupynambalucas/tupynambalucas/tree/main/cortex#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tupynambalucas/tupynambalucas.git",
    "directory": "cortex"
  },
  "scripts": {
    "cortex:core:up": "podman compose -f infrastructure/docker/compose.yaml --profile core up -d --build",
    "cortex:core:down": "podman compose -f infrastructure/docker/compose.yaml --profile core down",
    "cortex:mcp:up": "podman compose -f infrastructure/docker/compose.yaml --profile mcp up -d --build",
    "cortex:mcp:down": "podman compose -f infrastructure/docker/compose.yaml --profile mcp down",
    "cortex:agents:up": "podman compose -f infrastructure/docker/compose.yaml --profile agents up -d --build",
    "cortex:agents:down": "podman compose -f infrastructure/docker/compose.yaml --profile agents down",
    "cortex:core:reset": "podman compose -f infrastructure/docker/compose.yaml --profile core down --volumes && podman compose -f infrastructure/docker/compose.yaml --profile core up -d --build",
    "cortex:mcp:reset": "podman compose -f infrastructure/docker/compose.yaml --profile mcp down --volumes && podman compose -f infrastructure/docker/compose.yaml --profile mcp up -d --build",
    "cortex:agents:reset": "podman compose -f infrastructure/docker/compose.yaml --profile agents down --volumes && podman compose -f infrastructure/docker/compose.yaml --profile agents up -d --build",
    "cortex:antigravity:auth": "podman exec -it agent-antigravity agy",
    "cortex:copilot:auth": "podman exec -it agent-copilot copilot login"
  },
  "dependencies": {},
  "devDependencies": {}
}

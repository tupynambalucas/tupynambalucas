# Configuration reference

JSON schema reference for the agentgateway configuration file, plus IDE/editor schema validation.

The agentgateway configuration file is described by a [JSON
schema](https://agentgateway.dev/schema/config). This section explains how to wire that schema into
your editor for inline validation, and provides the complete generated reference for every field.

[Schema validation

Configure your IDE or editor to validate agentgateway YAML against the JSON schema.](validation.md)[Configuration schema explorer

Explore the agentgateway standalone configuration schema interactively, including nested fields and
…](schema.md)

[CEL expressions](/docs/standalone/latest/reference/cel/ 'CEL expressions')[Release notes](/docs/standalone/latest/reference/release-notes/ 'Release notes')

Was this page helpful?

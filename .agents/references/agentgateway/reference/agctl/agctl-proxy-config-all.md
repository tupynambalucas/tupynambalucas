# agctl proxy config all

Reference for the `agctl proxy config all` command.

Retrieve all Agentgateway configuration

### Synopsis

Retrieve all Agentgateway configuration.

```
agctl proxy config all [flags]
```

### Options

```
  -h, --help   help for all
```

### Options inherited from parent commands

```
  -f, --file string            Agentgateway config dump JSON file
  -k, --kubeconfig string      kubeconfig
  -n, --namespace string       Namespace to use when resolving resources
  -o, --output string          Output format: one of short|json|yaml (default "short")
      --proxy-admin-port int   Agentgateway admin port (default 15000)
```

### SEE ALSO

- [agctl proxy config](../agctl-proxy-config/) - Retrieve agentgateway configuration for a resource

[agctl proxy config](/docs/standalone/latest/reference/agctl/agctl-proxy-config/ 'agctl proxy config')[agctl proxy config backends](/docs/standalone/latest/reference/agctl/agctl-proxy-config-backends/ 'agctl proxy config backends')

Was this page helpful?

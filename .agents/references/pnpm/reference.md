How to Fix ItForce pnpm to use a flat, append-only reporter by passing --reporter=append-only to disable interactive elements that trap stream endings.Stream child logs explicitly using the --stream flag so output flushes directly without waiting for a buffer close.Run pnpm with --aggregate-output to group logs and release the stream handle uniformly only upon complete execution.Execute pnpm using an external native terminal window outside of VS Code to compare behavior if PTY allocation is misbehaving

---
title: 'MCP authentication'
description: 'Configure OAuth 2.0 and JWT authentication for MCP servers'
sidebar_position: 2
---

# MCP authentication

Configure OAuth 2.0 and JWT authentication for MCP servers

MCP authentication protects MCP servers with OAuth 2.0. Agentgateway acts as a resource server: it
validates the tokens that your authorization server issues, serves the protected resource metadata
that MCP clients discover, and adapts to the OAuth behavior of each supported identity provider.

For the policy reference, including the authorization server proxy, resource server only, and
passthrough scenarios, the supported providers, authentication modes, and JWT claim validation, see
[MCP authentication](/workspaces/cortex/reference/agentgateway/configuration/security/mcp-authn).

## Identity provider guides

For end-to-end setup with a specific identity provider, including registering the application and
connecting an MCP client, see the following guides.

[Auth0Use an Auth0 tenant and API as the authorization server.](/workspaces/cortex/reference/agentgateway/integrations/auth/auth0) [authentikUse a self-hosted authentik instance with a pre-registered client ID.](/workspaces/cortex/reference/agentgateway/integrations/auth/authentik) [DescopeUse a Descope project as the authorization server.](/workspaces/cortex/reference/agentgateway/integrations/auth/descope) [Microsoft Entra IDUse an Entra app registration, with metadata and registration bridging.](/workspaces/cortex/reference/agentgateway/integrations/auth/entra) [KeycloakUse a Keycloak realm, with proxied client registration.](/workspaces/cortex/reference/agentgateway/integrations/auth/keycloak) [OktaUse an Okta org authorization server with an explicit JWKS URL.](/workspaces/cortex/reference/agentgateway/integrations/auth/okta)

## Related

---
title: Studio & Creative Assets
sidebar_label: Creative Assets
sidebar_position: 4
---

### Centralized Creative Repository

The Studio workspace acts as the master vault for all creative assets of the tupynambalucas.dev brand. It centralizes master files from professional design and production suites, ensuring version-controlled visual evolution and eliminating fragmented asset management.

#### Design Tool Integration

The repository supports multi-tool workflows by maintaining raw source files that serve as the canonical origin for all production-ready assets.

- **Vector Graphics:** Adobe Illustrator (`.ai`) master files for logotypes, typography exploration, and iconography.
- **Digital Imaging:** Adobe Photoshop (`.psd`) sources for high-fidelity raster assets and marketing material.
- **3D Engineering:** Blender (`.blend`) master files for platform components and spatial visualizations (e.g., `Avokado.blend`).
- **Post-Production:** Adobe Premiere (`.prproj`) projects for community tutorials and marketing content.

#### Creative Asset Management

All high-fidelity master files are strictly maintained within the `creative/**` directory structure. This ensures that the entire history of the brand's visual identity is preserved and accessible within the monorepo context.

#### Production Pipeline

Master files transition to the application layers through an optimized pipeline:

- **Icons:** Sources are processed into optimized SVGs in `src/icons/`.
- **3D Models:** Blender master files are exported to GLB format for runtime rendering.
- **Tokens:** Visual constants are exported as strongly-typed TypeScript definitions from `src/tokens/`.

#### Cloudflare R2 Asset Synchronizer

To keep our Git repository lightweight, production-ready static assets are not committed directly to the codebase. Instead, they are managed and synchronized bidirectionally with Cloudflare R2 Object Storage using our dedicated CLI synchronizer package `@tupynambalucas-studio/assets/bucket`.

##### Dynamic Synchronization & Verification

The synchronization engine uses local MD5 hashing and remote ETag validation to guarantee zero redundant operations.

- **Registry Config:** The paths to sync are configured dynamically in `studio/assets/assets-manifest.json`.
- **Upload/Push:** Scans local assets, validates headers, and pushes new/modified static files to the R2 bucket.
- **Download/Pull:** Checks remote metadata, compares with local files, and pulls updated resources.

##### Operational Commands

Run the interactive Clack CLI directly from the root of the monorepo:

```bash
pnpm studio:bucket
```

From the CLI menu, choose **Push** to upload local changes or **Pull** to download the latest assets.

#### Complete Directory Structure

Below is the complete directory layout of the `studio/assets/` workspace:

```text
studio/assets/
├── creative/                 # High-fidelity master files (Ignored in Git, synced to tupynambalucas-creative)
│   ├── blender/              # Blender source files (.blend)
│   │   └── mockups/          # 3D mockup scenes
│   ├── illustrator/          # Illustrator sources (.ai)
│   │   └── logos/            # Vector logo project files
│   ├── photoshop/            # Photoshop sources (.psd)
│   │   ├── banners/          # PSD files for promotional banners
│   │   └── mockups/          # PSD files for device mockups
│   └── premiere/             # Premiere video edit source files (.prproj)
└── src/
    ├── brand/                # Canonical Brand Identity Assets (SVGs in Git, PNGs ignored/synced)
    │   ├── banners/          # Official brand banners & social OpenGraph covers (Ignored/synced)
    │   │   ├── banner-github.png
    │   │   └── og-default.png
    │   ├── favicons/         # Brand favicons and app touch icons (Ignored/synced)
    │   │   ├── apple-touch-icon.png
    │   │   ├── favicon.ico
    │   │   └── safari-pinned-tab.svg
    │   ├── guidelines/       # Brand styleguides and PDF booklets (Ignored/synced)
    │   ├── logos/            # Brand logos (SVGs in Git, PNGs ignored/synced)
    │   │   ├── logo-*.png
    │   │   └── logo-*.svg
    │   └── palettes/         # Brand color palettes (Ignored/synced)
    │       └── identity-palette.png
    ├── icons/                # Reusable SVG UI icons (Git-tracked code wrapper)
    │   └── index.tsx
    ├── images/               # Product UI raster assets (Ignored in Git, synced to tupynambalucas-assets)
    │   ├── illustrations/    # Decorative graphics and site artwork
    │   ├── marketing/        # Landing page promotional campaign assets
    │   └── mockups/          # Device frames & product preview screenshots (Ignored/synced)
    │       ├── mockup-desktop-dashboard.png
    │       └── mockup-mobile-app.png
    ├── three/                # WebGL/Three.js assets (Ignored in Git, synced to tupynambalucas-assets)
    │   ├── exr/              # HDRI environments
    │   └── models/               # Optimized GLB models
    └── tokens/               # Core design system tokens (Git-tracked code)
        ├── colors.ts
        ├── index.ts
        ├── theme.css
        └── tokens.css
```

---

### Brand Asset Namespace & Naming Standards

To maintain consistency, brand visual assets are organized under a centralized `brand/` directory structure and follow strict naming patterns.

#### Format & Storage Distribution Policy

:::info[Asset Storage Policy]

- **SVG Vector Files:** Committed directly to Git inside `studio/assets/src/brand/logos/` (and `src/brand/favicons/`). SVGs are text-based, lightweight, and can be compiled as dynamic React components.
- **PNG & Binary Files:** Kept out of Git to prevent repository bloat. They are stored under `studio/assets/src/brand/` and synchronized to the Cloudflare R2 CDN bucket via the CLI.
  :::

#### Brand Directory Subfolders

- **`brand/logos/`**: Vector and raster logotypes (e.g., `logo-horizontal-positive.svg`, `logo-mark-purple.png`).
- **`brand/palettes/`**: Visual color palette reference files (e.g., `identity-palette.png`).
- **`brand/favicons/`**: App favicons and mobile touch icons (e.g., `favicon.ico`, `apple-touch-icon-gradient.png`).
- **`brand/banners/`**: Official brand banners and social OpenGraph covers (e.g., `banner-github.png`, `og-default.png`).
- **`brand/guidelines/`**: Style guides, brand booklets, and reference manuals.

#### Logo Naming Conventions

All logo filenames must follow the pattern `logo-[lockup]-[variant].[ext]` using lowercase and hyphens:

##### 1. Base Lockups

- **`horizontal`**: Full logo mark with horizontal text lockup.
- **`mark`**: The brand symbol/icon only.

##### 2. Variants

- **`positive`**: Dark monochrome version (black or dark grey) optimized for light backgrounds.
  - _Example:_ `logo-horizontal-positive.svg` (React Component: `LogoHorizontalPositive`)
- **`negative`**: Inverted monochrome version (white) optimized for dark backgrounds.
  - _Example:_ `logo-horizontal-negative.svg` (React Component: `LogoHorizontalNegative`)
- **`[color]`**: Explicit English color names to differentiate brand palettes.
  - _Example:_ `logo-horizontal-purple.svg` (React Component: `LogoHorizontalPurple`)
- **`gradient`**: A vibrant signature color blend transition.
  - _Example:_ `logo-horizontal-gradient.svg` (React Component: `LogoHorizontalGradient`)
- **`expressive`**: Artistic visual style mark for custom landing pages.
  - _Example:_ `logo-horizontal-expressive.svg`
- **`multicolor`**: Multiple solid color blocks side-by-side.
- **`alt`**: Specialized layout adjustments or celebratory configurations.

#### Favicon Naming Conventions

- **Default Canonical**: `favicon.ico` (multi-resolution container packaged for standard tab support).
- **Themed Favicons**: `favicon-[variant].[ext]` (e.g., `favicon-positive.ico`, `favicon-gradient.png`).
- **Platform Specific**: `apple-touch-icon-[variant].png` (Apple touch icons) and `safari-pinned-tab-[variant].svg` (Safari pinned tab vectors).

---

#### Asset Resolution & Zero-Copy Architecture

To eliminate duplicate file storage in consuming applications like `@tupynambalucas-hub/web`:

- **In Development**: The Vite dev server intercepts requests starting with `/@studio-assets/` and serves the files dynamically from the local assets package directory on your machine.
- **In Production (Build-Time Copying)**: The application's build system uses `vite-plugin-static-copy` to automatically replicate files from `node_modules/@tupynambalucas-studio/assets/src/brand/` into the build output `dist/` directory at bundle time.

---

#### Namespaced Workspace Exports

Internal packages access creative assets via strictly defined exports to ensure architectural isolation:

- `@tupynambalucas-studio/assets/icons`: Optimized SVG library and curated Icon component registry.
- `@tupynambalucas-studio/assets/logos`: Canonical brand logotypes.
- `@tupynambalucas-studio/assets/brand/*`: Core brand assets (palettes, favicons, banners).
- `@tupynambalucas-studio/assets/tokens`: Strongly-typed design constants for JS/TS logic.
- `@tupynambalucas-studio/assets/theme.css`: Zero-runtime Tailwind CSS theme config.

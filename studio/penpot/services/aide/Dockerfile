# =================================================================== #
# studio/penpot — penpot-aide (MCP AI Assistant)
# Wraps the official penpotapp/mcp image.
# Build context: this directory (studio/penpot/services/aide).
# Ports 4400-4403: MCP server / plugin server interfaces.
# =================================================================== #

# --- Dev Profile ---
FROM penpotapp/mcp:2.17 AS dev

EXPOSE 4400
EXPOSE 4401
EXPOSE 4402
EXPOSE 4403

# --- Prod Profile ---
FROM penpotapp/mcp:2.17 AS prod

EXPOSE 4400
EXPOSE 4401
EXPOSE 4402
EXPOSE 4403

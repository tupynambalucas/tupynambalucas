# =================================================================== #
# studio/penpot — penpot-frontend
# Wraps the official penpotapp/frontend image (nginx + SPA static files).
# Build context: this directory (studio/penpot/services/frontend).
# =================================================================== #

# --- Dev Profile ---
FROM penpotapp/frontend:2.17 AS dev

EXPOSE 8080

# --- Prod Profile ---
FROM penpotapp/frontend:2.17 AS prod

EXPOSE 8080
